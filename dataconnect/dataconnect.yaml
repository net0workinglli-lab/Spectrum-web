specVersion: "v1"
serviceId: "spectrum"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "spectrum-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
